Execute the generate_queries function in `step_workflows.generate_queries.py`.  Use the path to the use case description file as the input to this one. 
Remember the path of the output file as it will be required for the next step.
After it is updated (or they choose not to), suggest running the `@generate-scenario-data` prompt.