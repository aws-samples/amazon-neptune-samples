# The 1G Playground server backs the local CLI client (see 1g-playground-cli, baseURL=http://localhost:8080/).
# Bind to the loopback interface only so the file load/export endpoints are not reachable from the network.
server.address=127.0.0.1
